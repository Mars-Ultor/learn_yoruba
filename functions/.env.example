# functions/.env.example
# Copy this to .env and fill in your values

# Comma-separated list of allowed CORS origins for the Firebase Functions API.
# Leave unset to allow all origins during local emulator development.
# e.g. ALLOWED_ORIGINS=https://your-app.web.app
ALLOWED_ORIGINS=http://localhost:5173
